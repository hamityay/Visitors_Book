- if params[:q].present?
  = render 'list'
- else
  .alert.alert-warning
    %p
      %h3= t('view.there_is_no_data', resource: Category.model_name.human.downcase)
      = link_to new_hq_country_path, class: 'btn btn-primary' do
        = t('action_button.new', resource_name: Category.model_name.human)
        = t('btn.add')