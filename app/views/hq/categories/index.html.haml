- content_for :toolbar do
  = link_to new_hq_category_path, class: 'btn btn-default' do
    %i.icon-plus
    = t('action_button.new', resource_name: Category.model_name.human)

.panel.panel-default.grid
  .panel-heading
    %i.fa.fa-table.icon-large
    = t('tt.index', resource_name: Category.model_name.human)
    .panel-tools
      .btn-group
        %a.btn{href: hq_categories_path, data: {toggle: 'toolbar-tooltip'}, title: t('view.reload')}
          %i.fa.fa-refresh
      .badge= @categories.total_entries
  =blankable(@categories)
  .panel-footer
    .pagination.pagination-sm
      = will_paginate @categories, renderer: BootstrapPagination::Rails, bootstrap: 3